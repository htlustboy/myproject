一、事务的隔离级别：

1.read uncommitted 读未提交（会出现脏读，不可重复读，幻读），隔离级别最低，性能最高

2.read committed 读提交（会出现不可重复读，幻读），sql server,oracle默认隔离级别

3.repeatable read 重复读（会出现幻读）mysql默认的隔离级别

4.serializable 序列化 ，隔离级别最高，性能最低

脏读:事务B读取了事务A未提交的数据,而且这个事务有可能回滚

不可重复读:事务A查询两次数据，第一次查询之后，事务B更新了数据并且提交，事务B再次查询时，数据发生了变化

幻读:事务A修改了表中的全部数据，同时事务B向表中添加了一条新的记录，结果事务A提交之后发现多出一条数据并没有被修改